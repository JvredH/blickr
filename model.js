
{errors.length > 0 ? <OpenModalButton buttonText={``}}
<OpenModalButton
										className="delete-spot"
										modalComponent={<DeletePlantModal />}
										buttonText="Delete Plant"
										/>
